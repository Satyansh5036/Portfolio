import React from 'react';
import ThemeToggle from './ThemeToggle';

const Navbar = () => (
  <header className="navbar">
    <span className="navbar-title">Welcome to My Portfolio</span>
    <ThemeToggle />
  </header>
);

export default Navbar;
